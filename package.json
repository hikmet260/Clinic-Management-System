{
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"pnpm -C packages/server start:dev\" \"pnpm -C packages/client dev\"",
    "dev:install": "pnpm -C packages/server install && pnpm -C packages/client install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
